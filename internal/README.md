# internal

Application internals grouped by layers:

- `app/`: bootstrap and wiring
- `config/`: configuration loading and validation
- `domain/`: core entities and domain errors
- `service/`: business logic
- `storage/`: persistence implementations
- `telegram/`: transport layer for Telegram bot I/O
